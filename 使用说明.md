# AI 模型网关 使用说明

> 本地运行的 AI 模型统一管理网关
> 端口 **18123** · Python/FastAPI · Tauri 桌面客户端（可选）
> 集成优化：熔断Fallback、配额感知、按Key过滤模型、供应商代理、别名表达式、全链路日志、系统提示词、健康面板、Token压缩、三协议互转、智能路由、自动探测优选

---

## 一、快速开始

### 方式 1：桌面客户端（推荐）
双击：
```
D:\opencodekejian\AI-Gateway-C\gateway-client\target\release\ai-gateway-c-client.exe
```
它会自动拉起网关服务并打开管理台窗口；关闭窗口只是缩到托盘（托盘右键可退出）。

### 方式 2：一键脚本
双击 `启动客户端.bat` —— 自动启动网关 + 用 Edge 打开网页版界面。

### 方式 3：手动命令行
```powershell
cd D:\opencodekejian\AI-Gateway-C
py -3 server.py
# 浏览器打开 http://127.0.0.1:18123/
```

### 首次安装依赖（只需一次）
```powershell
py -3 -m pip install fastapi uvicorn httpx
```

---

## 二、管理台五大功能页

双击打开客户端后，顶部有 5 个标签页。所有修改实时写入 `data/config.json` 并立即生效。

### 💬 聊天
| 控件 | 说明 |
|---|---|
| 左侧会话列表 | 多会话管理，「＋新建」创建；保存在浏览器本地 |
| 模型框 | 填具体模型 ID 或别名表达式（见第四节），默认 `auto` |
| 池下拉 | free / vip，走不同供应商池 |
| temp | temperature 采样温度（0–2） |
| max_tokens | 最大生成 token，留空用上游默认 |
| 发送 | Enter 直接发送；Shift+Enter 换行 |

回复为流式逐字输出；若模型返回思考链（如 hy3-free），会以「💭」灰字先显示。

### 🏭 供应商（核心管理页）
- 列表展示每家的协议、配额、Key 数、模型数、Fallback 链、熔断参数、启停状态。
- **＋新增供应商 / 编辑** 弹窗字段：
  - `ID`：唯一标识（英文）；`BaseURL`：以 `/v1` 结尾
  - `协议`：openai / anthropic / gemini（自动做协议转换）
  - `配额`：limited=免费源（调度时排在后面）、unlimited=付费优先
  - `代理`：单独给这家走代理，如 `http://127.0.0.1:7890`
  - `Fallback 链`：这家失败后依次尝试的其它供应商 ID（逗号分隔）
  - `熔断阈值/重置秒`：连续失败 N 次后熔断 M 秒不再请求它
- **Keys 编辑规则**（每行一把）：
  ```
  sk-xxxx                      ← 普通 Key，支持全部模型
  sk-yyyy | gpt-a,gpt-b        ← 这把 Key 只支持指定模型
  __KEYLESS__                  ← 免 Key 供应商标记
  ```
- **模型编辑规则**（每行一个）：
  ```
  gpt-4o-mini                   ← 默认启用
  deepseek-v4 | 128000 | 1      ← 附上下文长度、是否多模态(1)
  ```
- **测试连通 & 拉模型**：填好 BaseURL 和第一把 Key 后点它，能拉到上游 `/models` 列表；再点「⬇填入」自动填充模型列表。
- 操作列：编辑 / 启停（不停用配置只暂停调度）/ 删除（同时自动移出所有池）。

### 🗂️ 池管理
勾选每家供应商属于**免费池**还是**付费池**（可同时属于两个），点「保存池配置」生效。
调用时通过 `?pool=` 参数选择走哪个池。

### 📊 监控日志
- **请求量曲线**：近 30 分钟每分钟调用量柱状图（8 秒刷新），显示总数、峰值、各供应商占比。
- **健康卡片**：每家供应商的熔断状态（绿=正常/黄=熔断中/红=出错）、成功失败数、成功率进度条、最近延迟与最后错误。
- **全链路日志**：每次请求的 OUT(发出)/IN(返回)/FAIL(失败)/SKIP(熔断跳过)/COMPRESS(压缩) 全过程，可开自动刷新。

### ⚙️ 设置
| 设置项 | 对应优化 | 说明 |
|---|---|---|
| 全局系统提示词 | #7 | append 追加到现有 system 后 / override 覆盖 |
| Token 压缩 | #9 | 超过阈值自动截断(truncate)或摘要(summarize)早期对话 |

---

## 三、路由机制与接口定义

### 3.1 一条请求的路由决策链

每次对话请求按下面顺序处理（括号内为对应优化项）：

```
客户端请求  model=auto:free, ?pool=free
 │
 ① 入站协议识别      /v1/chat/completions 原样；/v1/messages(Anthropic)、
 │                   /v1beta/…:generateContent(Gemini) 自动转为内部格式 (#10)
 ② 系统提示词注入     append 追加 / override 覆盖 (#7)
 ③ Token压缩        超过 maxContextTokens 才触发，默认关闭 (#9)
 ④ 池过滤           只在 pools.free（或 vip）里的「启用」供应商中找
 ⑤ 别名与约束解析     auto:free → 解析成「免费供应商的全部启用模型」；
 │                   ctx>=N / vision 等条件在此过滤 (#5)
 ⑥ 配额感知排序       unlimited(付费) 排前，limited(免费) 排后 (#2)
 ⑦ 熔断检查          连续失败≥threshold 且未过 reset 秒 → 直接跳过该家 (#1)
 ⑧ 选 Key 轮询       该模型可用的 Key 之间轮询(round-robin)；
 │                   Key 带 supportedModels 时只允许那几个模型 (#3)
⑨ 出站适配          按供应商 protocol 转 openai/anthropic/gemini 报文 (#10)；
  │                   走该家专属 proxy(#4)
 │
 ├─ 成功 → 写健康统计(成功率/延迟/曲线)，返回结果
 └─ 失败 → 熔断计数+1 → 按 Fallback 链自动换下一家重试
            （候选用尽仍失败 → 返回 502 与最后错误）
```

**三个典型场景**

| 场景 | 发生什么 |
|---|---|
| 请求 `auto:free` | 免费池内所有启用模型成为候选 → 付费源不存在则全按 limited 排序 → 从第一家开始试 |
| nvidia 连续 403 三次 | 第 3 次后进入熔断 30 秒 → 期间请求自动落到 Fallback 链的下一家（pollinations → opencode-free） |
| 同一家配多把 Key | 每次请求在该家可用 Key 间轮询分摊用量；某把 Key 限定了 supportedModels，就只用于那些模型 |

### 3.2 接口路由定义

#### 对话类（OpenAI 兼容，第三方软件主要用这三个）

**`POST /v1/chat/completions`**
| 参数 | 位置 | 说明 |
|---|---|---|
| `pool` | query | `free` / `vip`，缺省用配置里的 `defaultPool` |
| `model` | body | 具体 ID 或别名表达式（见第四节） |
| `messages` | body | 标准 OpenAI 消息数组 |
| `stream` | body | `true`=SSE 流式，`false`=一次性 |
| `temperature` / `max_tokens` / `top_p` / `stop` | body | 可选，透传上游 |

```jsonc
// 请求
{ "model": "auto:free", "stream": false,
  "messages": [{"role":"user","content":"你好"}],
  "temperature": 0.7, "max_tokens": 500 }
// 响应（标准 OpenAI chat.completion 结构）
{ "choices":[{"message":{"role":"assistant","content":"…"}}], "usage":{…} }
// 失败：HTTP 502
{ "error": "全部候选失败，最后错误: …" }
```

**`GET /v1/models?pool=free`** → 当前池模型清单 + 支持的别名：
```jsonc
{ "object":"list",
  "data":[ {"id":"hy3-free","object":"model","owned_by":"opencode-free","quota":"limited"} ],
  "aliases":["auto","fast","daily","vision","auto:free","auto:paid","auto:ctx>=N","vision:free"] }
```

**`POST /v1/messages`** —— Anthropic 客户端入站：
```jsonc
// 请求（Anthropic 格式）
{ "model":"auto", "max_tokens":1024,
  "system":"你是助手", "messages":[{"role":"user","content":"Hi"}] }
// 响应（Anthropic message 格式）
{ "type":"message", "content":[{"type":"text","text":"…"}], "usage":{…} }
```

**`POST /v1beta/models/{模型}:generateContent`** —— Gemini 客户端入站：
```jsonc
// 请求（Gemini 格式）
{ "contents":[{"role":"user","parts":[{"text":"Hi"}]}],
  "generationConfig":{"maxOutputTokens":512} }
// 响应（Gemini 格式）
{ "candidates":[{"content":{"parts":[{"text":"…"}]}}] }
```
> 这两个入站路由会先转成内部 OpenAI 格式再走统一调度，响应再转回各自协议——即「三协议互转」(#10)。

**`POST /v1/responses`** —— OpenAI Responses API 客户端入站（Codex CLI 反向代理）：
```jsonc
// 请求（Responses 格式；input 支持字符串或消息数组，含 function_call 工具轮）
{ "model":"auto", "instructions":"你是助手",
  "input":[{"role":"user","content":[{"type":"input_text","text":"Hi"}]}],
  "max_output_tokens":512, "stream":true }
// 响应（Responses 格式，object=response）
{ "id":"resp_…", "object":"response", "status":"completed",
  "output":[{"type":"message","role":"assistant","content":[{"type":"output_text","text":"…"}]}],
  "usage":{…} }
// stream=true 时返回完整 SSE 事件序列：response.created / output_item.added /
// content_part.added / output_text.delta / function_call_arguments.delta /
// response.completed 等（喂给 Codex CLI）
```
> Codex CLI 接入：`codex --config` 里 `model_provider.base_url = http://127.0.0.1:18123/v1`、`wire_api = "responses"`、key 填任意值（如 `local`）；流式/工具调用与 Responses 协议一致性已实测。

#### 管理 API（管理台后台在用，也可脚本调用）

| 方法 & 路径 | 请求体 / 参数 | 响应 |
|---|---|---|
| `GET /api/config` | — | 整份 config.json |
| `POST /api/config` | 完整配置 JSON | `{"ok":true}` |
| `GET /api/health` | — | `{"suppliers":[{id,protocol,quota,breaker:{state,consecutive_fail},success,fail,rate,last_latency,last_error}]}` |
| `GET /api/metrics?minutes=30` | 分钟数(5–120) | `{"labels":["14:00",…],"counts":[0,3,…],"total":42,"per_supplier":{"opencode":30}}` |
| `GET /api/logs?lines=300` | 行数 | 纯文本日志尾部（OUT/IN/FAIL/SKIP/COMPRESS） |
| `POST /api/supplier/test` | `{"baseUrl":"https://x/v1","key":"sk-…"}` | `{"ok":true,"status":200,"count":35,"models":[…]}` 或 `{"ok":false,"error":"…"}` |
| `POST /api/suppliers/upsert` | 完整供应商对象 | `{"ok":true}` |
| `DELETE /api/suppliers/{id}` | — | `{"ok":true,"removed":1}`（同时移出所有池） |

**供应商对象字段速查**（upsert 用）：
```jsonc
{
  "id": "my-api", "name": "我的服务",
  "baseUrl": "https://xxx.com/v1",          // 以 /v1 结尾
  "protocol": "openai",                      // openai | anthropic | gemini
  "quota": "unlimited",                      // unlimited | limited(免费)
  "proxy": null,                             // 或 "http://127.0.0.1:7890" (#4)
  "fallback": ["pollinations"],              // 失败后依次尝试的家 (#1)
  "breaker": {"threshold": 3, "reset": 30},  // 熔断参数 (#1)
  "enabled": true,
  "keys": [ "sk-a",                          // 全模型可用
            {"key":"sk-b","supportedModels":["gpt-x"]},  // 限模型 (#3)
            "__KEYLESS__" ],                 // 免 Key 标记
  "models": [ {"id":"gpt-x","enabled":true,"contextWindow":128000,"multimodal":false} ]
}
```

**第三方客户端接入**（ChatBox / OpenWebUI 等）：
```
API Base : http://127.0.0.1:18123/v1
API Key  : 任意值（如 local）
模型名   : auto / auto:free / 具体模型ID
```

**CLI 反向代理接入**（借鉴 EchoBird 的 CLI 集成思路，走本网关三协议互转）：
```
Claude Code  : ANTHROPIC_BASE_URL=http://127.0.0.1:18123
               ANTHROPIC_AUTH_TOKEN=local  ANTHROPIC_MODEL=auto
opencode      : 自定义 provider baseUrl=http://127.0.0.1:18123/v1/messages
Codex CLI     : model_provider.base_url=http://127.0.0.1:18123/v1
               wire_api=responses  key=local  → POST /v1/responses
```
这些 CLI 无需各自直连免费源，全部由网关统一路由/熔断/回退/换 Key。

curl 示例：
```bash
curl http://127.0.0.1:18123/v1/chat/completions ^
  -H "Content-Type: application/json" ^
  -d "{\"model\":\"auto:free\",\"messages\":[{\"role\":\"user\",\"content\":\"你好\"}]}"
```

---

## 四、模型别名表达式（feat#5）

| 写法 | 含义 |
|---|---|
| `auto` | 在当前池里任选一家启用模型 |
| `fast` / `daily` / `vision` | 分组偏好；vision 只选多模态模型 |
| `auto:free` | 只在免费(limited/keyless)供应商中选 |
| `auto:paid` | 只在付费(unlimited)供应商中选 |
| `auto:ctx>=32000` | 只要上下文 ≥32K 的模型 |
| `auto:ctx<=8000&free` | 条件组合（用 & 连接） |
| `meta/llama-3.1-8b-instruct` | 直接写具体模型 ID |

调度顺序：满足条件的候选中 **付费优先于免费**；某家连续失败达到熔断阈值会被暂时跳过并自动 Fallback 到下一家。

---

## 五、文件与数据位置

```
AI-Gateway-C\
├─ server.py               主服务入口
├─ core\                   核心模块（路由/熔断/协议转换/压缩/探测…）
├─ data\config.json        ★ 所有配置都在这一个文件（可直接备份/手改）
├─ logs\gateway.log        全链路日志
├─ web\                    管理台 + 网页聊天页(/app)
└─ gateway-client\         Tauri 桌面客户端源码与构建产物
```

改了 `port` 等结构性字段需重启网关；其余（供应商/池/提示词/压缩）保存即生效。

---

## 六、常见问题（FAQ）

**Q：双击 exe 提示端口被占？**
之前的网关还在跑。PowerShell 执行：
```powershell
Get-NetTCPConnection -LocalPort 18123 | Select OwningProcess
Stop-Process -Id <上面的PID> -Force
```
或在「设置」把 port 改成别的（如 18124）后重启。

**Q：免费池报错 502？**
看「监控日志」最后错误。某上游频繁 401/402/403 时，多为接口地址变更或密钥失效，建议在「供应商」页核对地址，或在官方平台重新申请后换成你自己的 Key。

**Q：怎么加我自己的模型服务？**
供应商页 → 新增 → 填 BaseURL 和 Key（可用「添加模型源」的官方模板一键填地址）→ 点「测试连通&拉模型」→「⬇填入」→ 保存 → 池管理里勾进免费池或付费池。

**Q：想让它只走某个供应商？**
聊天页模型直接填那家的具体模型 ID；或临时把其它家「启停」掉。

**Q：客户端 exe 怎么重新打包？**
```powershell
cd D:\opencodekejian\AI-Gateway-C\gateway-client
cargo build --release     # 产物在 target\release\ai-gateway-c-client.exe
```

**Q：和 B 会冲突吗？**
不会。B 是 8670 端口、独立目录；C 是 18123、零引用 B 的任何文件，可同时运行。

---

*文档对应版本 v1.0.0 · 更新日期 2026-08-25*
