# AI 模型网关 使用说明

> 本地运行的 AI 模型统一管理网关
> 端口 **18111** · Python/FastAPI · Tauri 桌面客户端（可选）
> 集成优化：熔断Fallback、智能路由（EWMA 四维评分）、配额感知、按Key过滤模型、三协议互转（OpenAI/Anthropic/Gemini）、Token压缩、健康面板、全链路日志、官方模板一键填入

---

## 一、快速开始

### 方式 A：直接运行
```bash
cd <项目根目录>
pip install -r requirements.txt
python server.py
# 浏览器打开 http://127.0.0.1:18111/
```

### 方式 B：Docker
```bash
docker build -t aillm .
docker run -d -p 18111:18111 -v aillm-data:/data --name aillm aillm
```

### 方式 C：桌面客户端（可选）
```bash
cd gateway-client && npm install && npm run tauri dev
```

### 首次安装依赖（只需一次）
```bash
pip install -r requirements.txt
```

---

## 二、管理台功能页

启动后打开浏览器访问 `http://127.0.0.1:18111/`，顶部有多个标签页。所有修改实时写入 `data/config.json` 并立即生效。

### ➕ 添加模型
- 点开「接口模板」可选三大类：
  - **官方接口模板**：付费厂商，一键填入地址，Key 需你自行购买
  - **官方免费额度**：官方免费/限免档，Key 到官方网页注册领取
  - **本机/自建模型（免密钥）**：Ollama / LM Studio / vLLM 等本地服务，自动填入 `__KEYLESS__`
- 三步接入：填地址 → 填密钥 → 点测试。测试通过后自动获取模型列表。

### 🧩 模型列表
- 展示所有已启用模型及所属供应商、Key 状态
- 可按池（免费/付费）和能力（图像/视频/音频/代码/推理/工具）筛选

### 📊 实时监控
- **今日/本周/本月** Token 用量与节省金额
- **请求量曲线**：近 1 小时每分钟调用量柱状图
- **热门模型排行**：调用次数、输入/输出 Token 统计
- **最新日志**：OUT(发出)/IN(返回)/FAIL(失败)/SKIP(熔断跳过) 全过程

### 🧭 路由配置
- **智能路由**：开启后系统按任务难度自动匹配最佳模型（规则速判 + EWMA 四维评分）
- **自定义路由**：把多个模型组合成一个路由名，调用时填该名即限定在这些模型中调度

### 📖 调用方法
- 第三方客户端接入示例（ChatBox / Coze / Dify 等）
- CLI 反向代理接入（Claude Code / opencode / Codex CLI 等）

### ℹ️ 关于
- 版本信息、使用文档链接、开源地址

---

## 三、路由机制与接口定义

### 3.1 一条请求的路由决策链

```
客户端请求  model=auto
 │
 ① 入站协议识别      /v1/chat/completions 原样；
 │                   /v1/messages(Anthropic)、
 │                   /v1beta/…:generateContent(Gemini) 自动转为内部格式
 ② 智能路由          规则速判 + EWMA 评分，按任务选最佳模型
 ③ Token压缩         超过阈值自动截断或摘要（可关闭）
 ④ 模型匹配          按池过滤 → 别名解析 → 条件筛选
 ⑤ 配额感知排序      付费排前，免费排后
 ⑥ 熔断检查          连续失败达阈值 → 跳过，按 Fallback 链自动换下一家
 ⑦ 选 Key 轮询       该模型可用的 Key 间轮询分摊用量
 ⑧ 出站适配          按供应商协议转 openai/anthropic/gemini 报文
 │
 ├─ 成功 → 写健康统计，返回结果
 └─ 失败 → 熔断计数+1 → Fallback 链重试
            （候选用尽仍失败 → 返回 502）
```

### 3.2 接口路由定义

#### 对话类

**`POST /v1/chat/completions`**
| 参数 | 位置 | 说明 |
|---|---|---|
| `model` | body | 具体 ID 或别名表达式（见第四节），默认 `auto` |
| `messages` | body | 标准 OpenAI 消息数组 |
| `stream` | body | `true`=SSE 流式，`false`=一次性 |
| `temperature` / `max_tokens` | body | 可选，透传上游 |

**`GET /v1/models`** → 当前模型清单 + 别名列表

**`POST /v1/messages`** — Anthropic 客户端入站

**`POST /v1beta/models/{模型}:generateContent`** — Gemini 客户端入站

> 以上入站路由会先转成内部格式再走统一调度，响应再转回各自协议——即「三协议互转」。

#### 管理 API

| 方法 & 路径 | 说明 |
|---|---|
| `GET /api/config` | 获取整份 config.json |
| `POST /api/config` | 更新配置 |
| `GET /api/health` | 供应商健康状态 |
| `GET /api/metrics?minutes=30` | 用量统计 |
| `GET /api/logs?lines=300` | 全链路日志 |
| `GET /api/version` | 版本信息 |
| `POST /api/supplier/test` | 测试供应商连通性 |

---

## 四、模型别名表达式

| 写法 | 含义 |
|---|---|
| `auto` | 智能选择最佳模型 |
| `auto:free` | 只在免费(limited/keyless)供应商中选 |
| `auto:paid` | 只在付费(unlimited)供应商中选 |
| `auto:ctx>=32000` | 上下文 ≥32K 的模型 |
| `meta/llama-3.1-8b-instruct` | 直接写具体模型 ID |

调度顺序：满足条件的候选中 **付费优先于免费**；熔断的供应商会被暂时跳过。

---

## 五、文件与数据位置

```
项目根目录/
├─ server.py               主服务入口
├─ core/                   核心模块（路由/熔断/协议转换/压缩/探测…）
├─ data/config.json        所有配置都在这一个文件（可直接备份/手改）
├─ web/                    管理台前端
├─ gateway-client/         Tauri 桌面客户端（可选）
├─ docs/                   文档与截图
└─ packaging/              打包相关（deb/windows 安装包）
```

改了 `port` 等结构性字段需重启网关；其余（供应商/池/提示词/压缩）保存即生效。

---

## 六、常见问题（FAQ）

**Q：端口被占用？**
看哪个进程占了端口再停掉，或在 `data/config.json` 改 `port` 字段后重启：
```bash
# Linux/macOS
lsof -i :18111
kill <PID>

# Windows PowerShell
Get-NetTCPConnection -LocalPort 18111 | Select OwningProcess
Stop-Process -Id <PID> -Force
```

**Q：免费源报错 502？**
看管理台「实时监控」日志。免费源可能接口变更或额度用完，建议换成你自己的 Key。

**Q：怎么加模型？**
「➕ 添加模型」→ 用模板或手动填 BaseURL 和 Key → 测试连通 → 保存。

**Q：和别的版本会冲突吗？**
不会。本版默认 18111 端口、独立目录和数据，互不干扰。

---

*文档对应版本 v2.1.1 · 更新日期 2026-09-15*
